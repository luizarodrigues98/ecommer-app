<h1>E-Commerce App </h1>
<% if @warehouses.empty?%>
  <p> Nenhum galpão encontrado </p>
<%else%>
  <% @warehouses.each do |warehouse| %>
  <ul>
      <li><%= link_to warehouse.name, warehouse_path(warehouse.id) %></li>
      <p><%= warehouse.code %></p>
      <p><%= warehouse.city %></p>
    </li
  <% end %>
<% end %>
